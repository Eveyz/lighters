<div class="bg-light-green page-min-height">
  <%= render "admin/breadcrumb", domain: "addBooks" %>

  <div class="container">
    <%= react_component("container/courses/AssignBooksWidget", {
      course_id: @course.id,
      course_name: @course.name, 
      course_level: @course.level, 
      course_capacity: @course.capacity, 
      teacher: @teacher
    }) %>
    <br>
    <br>
  </div>
</div>